import './ScrollWrapper.css'

function RightVerticalScrollWrapper({children}){

    return(
        <div className="right-vertical-scroll-wrapper">
            {children}

        </div>
    );
    
}
export default RightVerticalScrollWrapper
